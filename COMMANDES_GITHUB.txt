═══════════════════════════════════════════════════════════════
  COMMANDES À EXÉCUTER MAINTENANT
═══════════════════════════════════════════════════════════════

Vous êtes prêt ! Git a déjà ajouté tous les fichiers.
Il ne reste que 4 commandes à exécuter :

═══════════════════════════════════════════════════════════════
  ÉTAPE 1 : Créer le dépôt sur GitHub
═══════════════════════════════════════════════════════════════

1. Aller sur : https://github.com/new

2. Remplir :
   - Repository name : legal-agenda
   - Description : Application d'agenda juridique collaborative
   - Visibilité : Private (recommandé)
   - ⚠️ NE PAS cocher "Add a README file"
   - ⚠️ NE PAS ajouter .gitignore

3. Cliquer sur "Create repository"

4. Copier l'URL qui s'affiche
   (ex: https://github.com/votre-username/legal-agenda.git)

═══════════════════════════════════════════════════════════════
  ÉTAPE 2 : Exécuter ces 4 commandes
═══════════════════════════════════════════════════════════════

Dans PowerShell (vous y êtes déjà) :

# 1. Créer le commit
git commit -m "Initial commit - Legal Agenda Application"

# 2. Ajouter le remote (remplacer USERNAME par votre username GitHub)
git remote add origin https://github.com/USERNAME/legal-agenda.git

# 3. Renommer la branche en main
git branch -M main

# 4. Pousser le code
git push -u origin main

═══════════════════════════════════════════════════════════════
  AUTHENTIFICATION
═══════════════════════════════════════════════════════════════

Si demande d'authentification :

Username : votre-username-github
Password : utilisez un Personal Access Token (PAS votre mot de passe)

Pour créer un token :
1. Aller sur : https://github.com/settings/tokens/new
2. Note : "Legal Agenda"
3. Cocher : repo (tous)
4. Generate token
5. Copier le token (vous ne le reverrez plus !)
6. Utiliser ce token comme mot de passe

═══════════════════════════════════════════════════════════════
  EXEMPLE COMPLET
═══════════════════════════════════════════════════════════════

# Si votre username GitHub est "john-doe"

git commit -m "Initial commit - Legal Agenda Application"
git remote add origin https://github.com/john-doe/legal-agenda.git
git branch -M main
git push -u origin main

═══════════════════════════════════════════════════════════════
  VÉRIFICATION
═══════════════════════════════════════════════════════════════

Après le push, aller sur :
https://github.com/votre-username/legal-agenda

Vous devriez voir tous vos fichiers ! 🎉

═══════════════════════════════════════════════════════════════
  EN CAS D'ERREUR
═══════════════════════════════════════════════════════════════

"Author identity unknown" :
  git config --global user.name "Votre Nom"
  git config --global user.email "votre-email@example.com"
  Puis recommencer le commit

"Authentication failed" :
  Utilisez un Personal Access Token (voir ci-dessus)

"Remote origin already exists" :
  git remote remove origin
  Puis recommencer la commande git remote add

═══════════════════════════════════════════════════════════════
  APRÈS LE PUSH
═══════════════════════════════════════════════════════════════

✅ Votre projet est sur GitHub !

Prochaines étapes :
1. Inviter des collaborateurs (Settings → Collaborators)
2. Voir docs/DEPLOY.md pour déployer en production
3. Commencer à développer !

═══════════════════════════════════════════════════════════════
